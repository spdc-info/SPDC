# GitHub Pages deployment for the existing Vite project
# Re-triggered after repository visibility was restored to public.
name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Check Node.js
        run: |
          node --version
          npm --version
      - name: Install dependencies
        run: npm install
      - name: Build project with Vite
        run: npm run build
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload built artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: dist

  deploy:
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
